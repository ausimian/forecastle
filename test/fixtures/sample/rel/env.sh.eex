#!/bin/sh

# Fixture-owned env.sh. Forecastle must append to this, not replace it, and its
# hook must come after this export - which runtime.exs reads, in the VM the
# launcher goes on to start.
export SAMPLE_ENV_MARKER=preserved
