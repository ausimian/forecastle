#!/bin/sh

# Fixture-owned env.sh. Forecastle must append to this, not replace it, and the
# Castle integration must run after it so that this export is visible to the
# preboot VM that expands runtime configuration.
export SAMPLE_ENV_MARKER=preserved
